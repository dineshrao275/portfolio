import Portfolio from "@/components/Portfolio";
import { phpPortfolioData } from "@/data/phpPortfolioData";

const PhpPortfolio = () => {
  return <Portfolio data={phpPortfolioData} />;
};

export default PhpPortfolio;
